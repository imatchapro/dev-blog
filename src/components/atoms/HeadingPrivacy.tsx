import React from 'react';

const HeadingPrivacy: React.FC = ({ children }) => {
  return <h2 className="text-gray-900 text-base font-semibold mb-4">{children}</h2>;
};

export default HeadingPrivacy;
